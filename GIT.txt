pwd: hiển thị vị tri
ls : hiển thị các thư mục
dir: hiển thị thư mục
ls -a:
mkdir : tạo thư mục
touch : tạo file cpp,...
rm -rf: xóa -))
echo -e "Abc\tdef" >> Bai2.txt : them chữ ( ghi tiếp)
echo -e "Abc\tdef" > Bai2.txt : them chữ ( ghi đè)
cat 1.txt: Hien thị nội dung trong file
du -sh :
cp: copy 
mv: move di chuyển
code . : mở vs code
*clear: xóa
*git log: hiển thị lịch sử
*git status: Kiểm tra trạng thái của file đã thay đổi trong thư mục làm việc
*git config --global user.name và git config --global user.email:Kiểm tra tên và email trong cấu hình
*git config --global user.name “FPT Shop” và git config --global user.email “fptshop@gmail.com”:Cài đặt tên hoặc email mới
*git add . : thêm tất cả các tệp đã thay đổi vào staging area
*git clone https://github.com/linhphuog/github.git : tải repo về máy tính
*git remote -v : kiểm tra kết nối với GitHub
*git remote add origin https://github.com/vietgit26/vietvippro.git
*git push -u origin main
*git config --list: xem danh sach